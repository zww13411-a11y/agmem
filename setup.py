from setuptools import setup, find_packages

setup(
    name="agmem",
    version="2.0.0",
    description="Five-Element driven Agent Memory Graph",
    packages=find_packages(),
    python_requires=">=3.11",
)
